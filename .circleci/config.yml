# Java Maven CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
  build:
    working_directory: ~/cf-ops-automation-broker
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - attach_workspace:
          at: ~/cf-ops-automation-broker
      - run:
          name: Build
          command: |
            mvn -q install:help --settings settings.xml
            mvn install --settings settings.xml
      - run:
          name: Save test results
          command: |
            mkdir -p ~/junit/
            find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} ~/junit/ \;
          when: always
      - store_test_results:
          path: ~/junit
      - store_artifacts:
          path: cf-ops-automation-cloudflare-broker/target/cf-ops-automation-cloudflare-broker-0.5.0-SNAPSHOT.jar
      - persist_to_workspace:
          root: .
          paths:
            - cf-ops-automation-cloudflare-broker/target/cf-ops-automation-cloudflare-broker-0.5.0-SNAPSHOT.jar
  build_n_deploy:
    working_directory: ~/cf-ops-automation-broker
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - attach_workspace:
          at: ~/cf-ops-automation-broker
      - run:
          name: deploy snapshots to bintray
          command: bin/deploy_to_bintray.sh
      - run:
          name: save artefacts
          command: |
            mkdir -p ./artefacts/
            find . -name '*.jar' -exec cp {} ./artefacts/ \;
          when: always
      - run:
          name: Save test results
          command: |
            mkdir -p ~/junit/
            find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} ~/junit/ \;
          when: always
      - store_test_results:
          path: ~/junit
      - store_artifacts:
          path: cf-ops-automation-cloudflare-broker/target/cf-ops-automation-cloudflare-broker-0.5.0-SNAPSHOT.jar
      - store_artifacts:
          path: JFrogPromotion.url
      - persist_to_workspace:
          root: .
          paths:
            - cf-ops-automation-cloudflare-broker/target/cf-ops-automation-cloudflare-broker-0.5.0-SNAPSHOT.jar
            - bin/release_to_github.sh
            - artefacts/*
  release_to_bintray:
    working_directory: ~/cf-ops-automation-broker
    docker:
      - image: circleci/golang:1.8.1
    steps:
      - attach_workspace:
          at: ~/cf-ops-automation-broker
      - run:
          name: release to bintray
          command: bin/release.sh
  release_to_github:
    working_directory: ~/cf-ops-automation-broker
    docker:
      - image: circleci/golang:1.8.1
    steps:
      - attach_workspace:
          at: ~/cf-ops-automation-broker
      - run:
          name: release artefacts to github
          command: |
          for f in ./artefacts/*
          do
            echo "Processing $f file..."
            # take action on each file.
            release_to_github.sh github_api_token=$GITHUB_TOKEN owner=$CIRCLE_PROJECT_USERNAME repo=$CIRCLE_PROJECT_REPONAME tag=$CIRCLE_TAG filename=./artefacts/$f
          done
workflows:
  version: 2
  build-n-deploy:
    jobs:
      - build:
          filters:
            branches:
              ignore: develop
      - build_n_deploy:
          filters:
            branches:
              only: develop
            tags:
              only: /.*/
      - release_to_github:
          requires:
            - build_n_deploy
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /.*/
